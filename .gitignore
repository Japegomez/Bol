# Local env and build artifacts at repo root
.env
.env.*
!.env.example

# Cursor session artifacts (rules are tracked)
.cursor/

# Apple Sign-In private keys (never commit)
*.p8
AuthKey.p8

# Supabase CLI temp
supabase/.temp/
/scripts/
!/scripts/generate_apple_jwt.py

# Graphify
/graphify-out

# Firebase CLI cache
.firebase/

# Firebase Cloud Functions (local npm install; not used on Spark plan)
functions/node_modules/

# Local share/OG test artifacts
fb_test.html
v4.html
img_test.jpg
